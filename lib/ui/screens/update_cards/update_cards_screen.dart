import 'package:flutter/material.dart';

class UpdateCardsScreen extends StatelessWidget {
  const UpdateCardsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:  AppBar(
        title:  const Text("Update Cards Screen"),
        centerTitle: true,
      ),
    );
  }
}
